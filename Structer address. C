#include<stdio.h>
 struct adress{
char city;
int pin;
};
  struct person{
char name[50];
int pin;
struct address addr;
  };
int main() {
  struct person p;
  printf("entr the name\n");
  scanf("%s\n",p.name);
  printf ("enter the city name");
    scanf("%s\n",p.addr.city);
  printf ("enter the pin\n");
  scanf("%s\n",&p.addr.pin);
  printf("\nperson details\n") ;
  printf ("name:%s\n city:%s\npin:%d\n ", p. name.city. 
